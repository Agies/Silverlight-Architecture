<UserControl x:Class="SilverlightArchitecture.Views.ExampleView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300" d:DesignWidth="400">
    
    <Grid x:Name="LayoutRoot" Background="White">
        <Grid.ColumnDefinitions >
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <!--Badly Named Binding-->
        <TextBlock Grid.ColumnSpan="2" Text="{Binding Path=EmployeeName}" FontSize="16" FontWeight="Bold" />
        <TextBlock Grid.Row="1" Text="First Name:" />
        <TextBlock Grid.Row="2" Text="Last Name:"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Path=FirstName, Mode=TwoWay}" />
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Path=LastName, Mode=TwoWay}" />
    </Grid>
</UserControl>
